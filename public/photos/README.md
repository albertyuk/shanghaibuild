# Location photos

Drop image files here and reference them from `src/data/chapters.ts` (or
the private editor at `/edit.html`) as `/photos/filename.jpg`.

Each location pin has up to two photo slots. A pin with photos gets an
Ace Combat-style sat-photo callout panel in the map pane while its
chapter is active; pins without photos render nothing extra.
